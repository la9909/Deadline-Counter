# 과제 마감 카운터 MVP PRD


## 1) 목표와 성공조건

### 목표

대학생이 **과제명과 마감일을 입력하면 남은 기간을 D-Day 형태로 즉시 확인할 수 있는 단일 화면 서비스**를 만든다.

이번 MVP에서는 여러 기능을 넣지 않고, **과제 1개를 등록하고 마감일까지 남은 날짜를 보여주는 기능 1개**만 구현한다.

### 핵심 기능

**과제 마감일 등록 및 D-Day 표시**

사용자가 다음 정보를 입력하고 `과제 등록` 버튼을 누르면 같은 화면에 과제 카드가 생성된다.

* 과제명
* 마감일

### 성공 조건

아래 흐름이 한 화면에서 정상적으로 동작하면 성공으로 판단한다.

1. 사용자가 과제명을 입력한다.
2. 마감일을 선택한다.
3. `과제 등록` 버튼을 누른다.
4. 입력한 과제명이 화면에 표시된다.
5. 마감일까지 남은 기간이 `D-3`, `D-Day` 등의 형태로 표시된다.

### 제외 범위

이번 MVP에서는 다음 기능을 구현하지 않는다.

* 회원가입
* 로그인
* 소셜 로그인
* 결제
* 구독
* 데이터베이스 저장
* 서버 저장
* 새로고침 후 데이터 유지
* 과목별 분류
* 시간표 연동
* 캘린더 연동
* 알림 기능
* 이메일 알림
* 푸시 알림
* 과제 파일 업로드
* 친구와 과제 공유
* 과제 수정 기능
* 과제 삭제 기능
* 완료 체크 기능

---

## 2) 사용자 스토리

### 핵심 사용자

여러 과제의 마감일을 놓치지 않고 남은 기간을 빠르게 확인하고 싶은 대학생

### 사용자 스토리

> 대학생으로서 과제명과 마감일을 입력해 남은 날짜를 D-Day로 확인하고 싶다. 그래야 과제 마감이 얼마나 남았는지 빠르게 파악할 수 있다.

### 기본 사용 흐름

1. 사용자가 화면에 접속한다.
2. 과제 등록 영역을 확인한다.
3. 과제명을 입력한다.
4. 마감일을 선택한다.
5. `과제 등록` 버튼을 누른다.
6. 같은 화면에 과제 카드가 생성된다.
7. 카드에서 과제명, 마감일, D-Day를 확인한다.

---

## 3) 화면 명세

### 화면 수

**1개**

### 화면 이름

`과제 마감 카운터`

### 화면 구성

화면은 위에서 아래로 다음 순서로 구성한다.

### A. 헤더 영역

표시 내용:

* 제목: `과제 마감 카운터`
* 설명: `마감일까지 얼마나 남았는지 확인해보세요.`

별도의 메뉴나 다른 페이지로 이동하는 버튼은 만들지 않는다.

---

### B. 과제 등록 영역

#### 과제명 입력

* 한 줄 텍스트 입력창을 제공한다.
* 최대 50자까지 입력할 수 있다.
* 필수 입력값이다.

입력 예시:

`정보보안 보고서`

#### 마감일 입력

* 날짜를 선택할 수 있는 입력 영역을 제공한다.
* 필수 입력값이다.
* 오늘 날짜 이상만 등록할 수 있다.

#### 과제 등록 버튼

버튼 문구:

`과제 등록`

과제명과 마감일이 정상적으로 입력된 경우에만 과제를 등록한다.

---

### C. 과제 표시 영역

정상적으로 등록된 과제를 카드 형태로 표시한다.

카드에는 다음 정보를 표시한다.

* 과제명
* 마감일
* D-Day

표시 예시:

```text
정보보안 보고서

마감일
2026.09.01

D-6
```

마감일이 오늘인 경우:

```text
D-Day
```

### 빈 상태

등록된 과제가 없는 경우 다음 문구를 표시한다.

`등록된 과제가 없습니다.`

`마감일을 확인할 과제를 등록해보세요.`

---

## 4) 기능 명세

### 기능명

**과제 마감일 등록 및 D-Day 계산**

### 기능 목적

사용자가 과제명과 마감일을 입력하면 현재 날짜를 기준으로 마감일까지 남은 날짜를 계산하여 보여준다.

### 입력값

| 항목  | 필수 여부 | 조건          |
| --- | ----- | ----------- |
| 과제명 | 필수    | 1~50자       |
| 마감일 | 필수    | 오늘 또는 미래 날짜 |

### 실행 조건

사용자가 `과제 등록` 버튼을 누른다.

### 정상 처리

입력값이 모두 정상인 경우 다음 순서로 처리한다.

1. 과제명과 마감일을 확인한다.
2. 현재 날짜와 마감일의 차이를 계산한다.
3. 남은 날짜를 D-Day 형식으로 변환한다.
4. 화면에 과제 카드를 표시한다.
5. 등록 성공 후 입력창을 초기화한다.

### D-Day 표시 규칙

마감일까지 7일 남은 경우:

```text
D-7
```

마감일까지 1일 남은 경우:

```text
D-1
```

마감일이 오늘인 경우:

```text
D-Day
```

### 데이터 형태

각 과제는 다음 정보만 가진다.

```text
과제
- 과제명
- 마감일
- 남은 날짜
```

별도의 사용자 ID나 서버 ID는 만들지 않는다.

### 데이터 유지 범위

현재 화면이 열려 있는 동안에만 데이터를 유지한다.

페이지를 새로고침하면 등록된 과제가 사라져도 된다.

---

## 5) 예외 처리

### 5-1. 과제명을 입력하지 않은 경우

**조건**

과제명이 비어 있는 상태에서 `과제 등록` 버튼을 누른다.

예:

```text
""
```

**처리**

* 과제를 등록하지 않는다.
* 마감일 입력값은 유지한다.
* 과제명 입력 영역에 오류 상태를 표시한다.
* 다음 안내 문구를 표시한다.

`과제명을 입력해주세요.`

사용자가 과제명을 정상적으로 입력하면 오류 메시지를 제거한다.

---

### 5-2. 과제명에 공백만 입력한 경우

**조건**

과제명에 실제 문자는 없고 공백만 입력되어 있다.

예:

```text
" "
"     "
```

**처리**

공백만 있는 값은 입력하지 않은 것으로 판단한다.

* 과제를 등록하지 않는다.
* 입력값을 임의로 등록하지 않는다.
* 다음 오류 메시지를 표시한다.

`과제명을 입력해주세요.`

과제명 앞뒤에 불필요한 공백이 있는 경우에는 등록 전에 제거한다.

예:

```text
"  정보보안 보고서  "
```

실제 표시:

```text
정보보안 보고서
```

---

### 5-3. 과제명이 50자를 초과하는 경우

**조건**

사용자가 50자를 초과하여 과제명을 입력하려 한다.

**처리**

* 과제명은 최대 50자까지만 입력할 수 있도록 제한한다.
* 50자를 초과한 문자는 입력값에 반영하지 않는다.
* 현재 글자 수를 표시할 수 있다.

예:

```text
32 / 50
```

50자에 도달하면 추가 입력은 허용하지 않는다.

---

### 5-4. 마감일을 선택하지 않은 경우

**조건**

마감일이 비어 있는 상태에서 `과제 등록` 버튼을 누른다.

**처리**

* 과제를 등록하지 않는다.
* 정상적으로 입력한 과제명은 유지한다.
* 날짜 입력 영역에 오류 상태를 표시한다.
* 다음 메시지를 표시한다.

`마감일을 선택해주세요.`

사용자가 정상적인 마감일을 선택하면 해당 오류 메시지를 제거한다.

---

### 5-5. 오늘보다 이전 날짜를 마감일로 선택한 경우

**조건**

현재 날짜보다 이전 날짜를 마감일로 선택한다.

예를 들어 오늘이 `2026-08-26`이라면 다음 날짜는 등록할 수 없다.

```text
2026-08-25
2026-08-10
2025-12-31
```

**처리**

* 과제를 등록하지 않는다.
* 입력한 과제명은 유지한다.
* 날짜 입력 영역에 오류 메시지를 표시한다.

`지난 날짜는 마감일로 등록할 수 없습니다.`

### 허용 범위

* 과거 날짜: 허용하지 않음
* 오늘 날짜: 허용
* 미래 날짜: 허용

---

### 5-6. 마감일이 오늘인 경우

**조건**

사용자가 현재 날짜를 마감일로 선택한다.

**처리**

오류로 처리하지 않는다.

정상적으로 과제를 등록하고 D-Day 영역에 다음과 같이 표시한다.

```text
D-Day
```

`D-0`으로 표시하지 않는다.

---

### 5-7. 여러 입력값이 동시에 잘못된 경우

**조건**

과제명과 마감일이 모두 비어 있는 상태 등 여러 오류가 동시에 존재한다.

예:

```text
과제명: 없음
마감일: 없음
```

**처리**

첫 번째 오류만 표시하지 않고 문제가 있는 입력 항목마다 각각 오류 메시지를 표시한다.

과제명:

`과제명을 입력해주세요.`

마감일:

`마감일을 선택해주세요.`

사용자가 하나의 오류만 수정했다면 수정된 항목의 오류 메시지만 제거한다.

나머지 오류는 그대로 유지한다.

---

### 5-8. 과제 등록 버튼을 빠르게 여러 번 누르는 경우

**조건**

사용자가 `과제 등록` 버튼을 짧은 시간에 여러 번 연속으로 누른다.

**처리**

동일한 클릭으로 과제 카드가 여러 개 생성되지 않도록 한다.

한 번의 정상적인 등록 행동당 과제 카드는 1개만 생성한다.

등록 처리가 진행되는 동안 동일 데이터가 중복 등록되어서는 안 된다.

---

### 5-9. 같은 과제명과 같은 마감일을 다시 입력한 경우

**조건**

이미 화면에 표시된 과제와 동일한 과제명과 마감일을 다시 등록한다.

예:

기존 등록:

```text
정보보안 보고서
2026.09.01
```

새로운 입력:

```text
정보보안 보고서
2026.09.01
```

**처리**

중복 과제가 생성되지 않도록 등록을 막는다.

다음 안내 문구를 표시한다.

`이미 등록된 과제입니다.`

기존에 등록된 카드는 그대로 유지한다.

---

### 5-10. 같은 과제명이지만 마감일이 다른 경우

**조건**

과제명은 같지만 마감일이 다르다.

예:

```text
정보보안 보고서 / 2026.09.01
정보보안 보고서 / 2026.09.15
```

**처리**

서로 다른 과제로 판단하여 등록을 허용한다.

과제명만으로 중복 여부를 판단하지 않는다.

---

### 5-11. 정상 등록 후 입력값 처리

**조건**

과제가 정상적으로 등록되었다.

**처리**

다음 입력값을 초기화한다.

* 과제명
* 마감일
* 과제명 글자 수
* 입력 오류 메시지

등록 실패 시에는 정상적으로 입력한 값을 초기화하지 않는다.

예를 들어 과제명은 정상이고 날짜만 비어 있는 경우:

```text
과제명 → 유지
마감일 → 오류 표시
```

사용자가 날짜만 추가하여 다시 등록할 수 있어야 한다.

---

### 5-12. 등록된 과제가 없는 경우

**조건**

사용자가 처음 화면에 들어왔거나 등록된 과제가 없는 상태이다.

**처리**

빈 카드나 오류 메시지를 표시하지 않는다.

대신 다음 안내 문구를 표시한다.

`등록된 과제가 없습니다.`

`마감일을 확인할 과제를 등록해보세요.`

첫 번째 과제가 정상적으로 등록되면 빈 상태 문구를 제거하고 과제 카드를 표시한다.

---

## 6) 완료 조건

아래 항목을 모두 충족하면 MVP 구현 완료로 판단한다.

* [x] 화면은 하나만 존재한다.
* [x] 별도의 페이지 이동이 없다.
* [x] 과제명을 입력할 수 있다.
* [x] 과제명은 최대 50자로 제한된다.
* [x] 마감일을 선택할 수 있다.
* [x] `과제 등록` 버튼이 존재한다.
* [x] 정상 입력 후 과제 카드가 생성된다.
* [x] 카드에 과제명이 표시된다.
* [x] 카드에 마감일이 표시된다.
* [x] 카드에 D-Day가 표시된다.
* [x] 마감일이 오늘이면 `D-Day`로 표시된다.
* [x] 과거 날짜는 등록할 수 없다.
* [x] 과제명이 비어 있으면 등록할 수 없다.
* [x] 공백만 입력한 과제명은 등록할 수 없다.
* [x] 같은 과제명과 같은 날짜의 중복 등록을 막는다.
* [x] 연속 클릭으로 동일한 과제가 여러 번 생성되지 않는다.
* [x] 정상 등록 후 입력값이 초기화된다.
* [x] 등록 실패 시 정상적으로 입력된 값은 유지된다.
* [x] 등록된 과제가 없을 때 빈 상태 문구가 표시된다.
* [x] 로그인 기능이 없다.
* [x] 결제 기능이 없다.
* [x] 데이터베이스를 사용하지 않는다.
* [x] 새로고침 후 데이터 유지 기능을 만들지 않는다.

---

## 7) Phase 2: Google Gemini AI 서비스 기능 확장

### 핵심 기능
1. **AI 자연어 과제 빠른 입력 (Natural Language Assignment Parser)**
   - 자연어 입력문(예: *"다음주 금요일 18시까지 컴퓨터 구조 보고서 제출"*)을 Gemini AI(`gemini-3.6-flash`)가 분석하여 과제명과 마감 날짜(YYYY-MM-DD)를 파싱하고 등록 폼에 자동 반영.
2. **AI 과제 단계별 실행 가이드 (Task Breakdown Assistant)**
   - 과제 카드에서 **"✨ AI 과제 가이드"** 클릭 시, 해당 과제의 주제 및 D-Day 긴급도를 기반으로 **D-Day 맞춤 한줄 팁**, **오늘 꼭 해야 할 일**, **추천 3단계 실행 가이드 및 예상 소요 시간**을 실시간 생성.
3. **보안 및 아키텍처**
   - API 키는 서버 사이드 환경변수(`GEMINI_API_KEY`)를 통하여 안전하게 처리되며 `.gitignore`로 보호됨.

